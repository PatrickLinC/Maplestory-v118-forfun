# 楓之谷 v118 私服開發日記

> 記錄 TMS v119.1 私服從架設到自訂開發的歷程(高層次)。
> 招牌的 PoE 附魔玩法之細節屬本服特色,不在此揭露,僅記錄「做了這件事」。

---

## 2026-09-01 — 伺服器架起來了

- 以 **TWMS_118 原始碼**(實際版本 v119.1,搭 v118 客戶端)在 Windows 11 上架設成功,通過握手封包測試。
- 佈局:`MapleServer_v119.1\`(自備 Temurin 8u504 JRE + MariaDB 10.11.19 免安裝版),資料庫資料放獨立目錄。
- **兩個關鍵踩雷點**:
  1. `-Dwzpath` 必須以 `/` 結尾(原始碼直接字串相接 `wz/Mob.wz`,不補分隔符)。
  2. WZ 解包後檔名要是 `<名稱>.img.xml`(`getData("Mob.img")` 內部會再接 `.xml`)。

## 2026-09-02 — 客戶端相容 + 開始自訂開發

- 解決了 v118 客戶端在 Win11 上的**保護機制與繪圖相容問題**,成功創角進遊戲(細節略)。
- 客戶端靠解析官方登入網域連線,系統 hosts 加一行指到伺服器 IP 即可,不用改客戶端。
- **借鏡其他私服的特色**,開始在自己的伺服器上設計玩法(主打 PoE 風格附魔、分段經驗等)——重現風格而非拷貝。
- 開發環境獨立放在 `C:\MapleDev`;改伺服器用 **Javassist 位元碼注入**(定義集中在 `Patcher.java`),CFR 反編譯只用來讀。
- 已完成的通用功能:分段經驗、背包全開、寵物技能全開、自訂指令(`@fm`、`!go` 傳送樞紐、`@ea` 解卡+中文狀態、`@help` 中文化)、NPC 對話卡住自動提示。

## 2026-09-03 — NPC 腳本

- NPC 是外部 JS 腳本(Nashorn),點擊即時讀檔、改腳本免重啟(玩家重登生效)。
- 釐清 JS API:取穿戴裝備要用 `getInventory(EQUIPPED).getItem(負數欄位)`;改完用 `forceReAddItem` + `equipChanged()` 更新客戶端並重算。
- 附魔石設計成**全新專屬道具**(不動任何原廠捲軸),攔截點在 `InventoryHandler.UseUpgradeScroll`。

## 2026-09-04 — PoE 附魔系統上線

- 完成招牌的 **PoE 風格附魔石／潛能系統**:24 種詞綴、10 種附魔石(賦予/重洗/加排/污染),搭配掉落、NPC 販售與 Boss 獎勵箱。玩法介紹見 [docs/PoE附魔系統.md](docs/PoE附魔系統.md);**實作原始碼不公開**。
- 過程中建立的**通用技術**:
  - **WZ 程式化編輯**(HaRepacker 2023 的 MapleLib):客戶端加密是 `EMS`;`SaveToDisk` 第二參數要傳 `(bool?)false`;捲軸節點 `DeepClone()` 會無限遞迴爆棧,要從零 `new`。
  - **NPC 腳本編碼坑**:含中文的腳本必須存 **UTF-8 with BOM**,否則伺服器按檔頭當 GBK 解成亂碼。

## 2026-09-05 — v120 繪圖卡死,確定走 v118

- 為了讓附魔「像原生潛能一樣顯示」,試著升 v120/v122 客戶端。**協定完全相容**(能連上、無 Ping 超時),但**客戶端在開發機就是繪圖出不來**(黑屏 / 亂碼 Error),多種相容層/色深/相容模式全試過無效。
- 同機 **v118 客戶端繪圖完全正常** → 是那個 v120 build 的問題。**決定:顯示相關功能改走 v118 方案。**

## 2026-09-06～07 — 改 exe:把附魔區塊畫到 tooltip 上

- v118 客戶端原生不畫潛能行。決定**改客戶端 exe**:解殼 dump,用 MinGW-w64 i686 編 inline-hook DLL,hook 在裝備 tooltip 函式結尾,讀取 item 上某欄位所編碼的資料、翻譯後畫到素質下方(通用的 tooltip 顯示技術;詞綴內容不公開)。
- 一鍵啟動器 + 自動注入(解殼穩定後才注入;中文路徑要 `LoadLibraryW`)。
- 順手把**史匹奈亞世界旅遊 NPC(9000020)**重寫成四國選單。
- 確認顯示在所有裝備類型都正常。

## 2026-09-07 — 確認數值加成正確套用

- 追伺服器戰鬥碼,確認附魔的數值加成有**正確套進戰鬥結算**(細節略)。
- 釐清一個 v118 限制:客戶端會丟棄潛能資料,故某些「乘算型」加成雖實際生效,但螢幕跳出的傷害數字不會反映;平值型(攻擊力/主屬性等)則跳字與實際皆含。
- `@ea` 加上即時戰鬥數值讓玩家眼見為憑,並移除與 tooltip 重複的資訊。

## 2026-09-08 — 嘗試讓跳字反映加成(客戶端逆向)

- 目標:hook 客戶端「畫傷害數字」的函式,讓螢幕跳字與實際傷害一致。
- 裝 **radare2** 做反組譯分析:校準座標系、找攻擊封包 opcode、call graph 排行、字串錨點。
- 結論:這個去符號又最佳化的 build,**runtime 戰鬥/繪圖碼沒有字串錨點**,啟發式線索多為載入器或假陽性,沒有反編譯器很難收斂。**此項暫停**(加成本來就有生效,這純是視覺對齊)。

## 2026-09-10～11 — 備份上 GitHub

- 把一路寫的**可公開技術程式碼與開發日記**整理上 GitHub,並排除所有版權/敏感/二進位檔。
- **玩法有寫成介紹文件([docs/PoE附魔系統.md](docs/PoE附魔系統.md)),但實作原始碼(引擎/數值/整合掛鉤)不公開**;需要交流實作請私訊。
